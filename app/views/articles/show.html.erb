<h1><%= @article.title %></h1>

<%= link_to "Edit", edit_article_path %> || <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>

<%= @article.content %>

<%= link_to "Back", articles_path %>
